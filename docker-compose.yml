services:
  url-checker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - ./:/usr/src
    command: ['node', '/app/build/index.js', 'https://youtube.com']
